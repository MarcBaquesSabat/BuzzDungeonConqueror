#include "Renderer.h"

void BuzzRPG::Renderer::LoadImage(char* filePath)
{
	/*SDL_Surface* imageSurface = IMG_Load(filePath);
	SDL_Texture* imageTexture = SDL_CreateTextureFromSurface(renderer, imageSurface);
	SDL_FreeSurface(imageSurface);*/
}
